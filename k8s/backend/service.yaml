# ── Backend Service ───────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: sk014-backend-svc
spec:
  selector:
    app: sk014-backend
  ports:
    - port: 8080
      targetPort: 8080